'use strict'

import React from 'react-native'

var {
  StyleSheet
} = React

var styles = StyleSheet.create({
  btn: {
    paddingHorizontal: 20,
    paddingVertical: 5,
    backgroundColor: '#369',
    borderRadius: 3,
  },
  btnText: {
    color: 'white'
  }
})

export default styles
